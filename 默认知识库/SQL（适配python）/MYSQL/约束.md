---
title: 约束
url: https://www.yuque.com/ehsuh/oguki0/kgdvrgp4mfaxanw6
doc_id: 216388618
exported_at: 2026-09-12T10:06:58
---

auto_increment

能自动自增

一个字段可以添加多个约束

<!-- 这是一张图片，ocr 内容为：概述 1.概念:约束是作用于表中字段上的规则,用于限制存储在表中的数据. 2.目的:保证数据库中数据的正确,有效性和完整性. 3.分类: 约束 关键字 描述 限制该字段的数据不能为NULL 非空约束 NOT NULL 唯一约束 UNIQUE 保证该字段的所有数据都是唯一,不重复的 主键约束 主键是一行数据的唯一标识,要求非空且唯一 PRIMARY KEY 保存数据时,如果未指定该字段的值,则采用默认值 默认约束 DEFAULT 保证字段值满足某一个条件 CHECK 检查约束(8.0.16版本之后) 用来让两张表的数据之间建立连接,保证数据的一致性和完整性 FOREIGN KEY 外键约束 注意:约束是作用于表中字段上的,可以在创建表/修改表的时候添加约束. -->
![](https://cdn.nlark.com/yuque/0/2025/png/52131016/1745222837530-65f8cef5-1174-41fe-894d-ba8a799fc26d.png)



在主键自增的时候存在null不算， 只有重复时主键加



<!-- 这是一张图片，ocr 内容为：CREATE TABLE USER( ID INT PRIMARY KEY AUTO_INCREMENT COMMENT 主键', NAME VARCHAR(10) NOT NULL UNIQUE COMMENT'姓名', AGE INT CHECK(AGE>0&AGE<> 120 ) COMMENT'年龄', STATUS CHAR(1) DEFAULT'1 COMMENT 状态', GENDER CHAR(1)COMMENT'性别' COMMENT用户表"I --插入数据 INSERT INTO USER(NAME,AGE,STATUS,GENDER) VALUES ('TOM1',19,'1','''''''''''''TOM2',25,''''罗''罗')')] INSERT INTO USER(NAME,AGE,STATUS,GENDER) VALUES ('TOM3',19,'1','男'); INSERT INTO USER(NAME,AGE,STATUS,GENDER) VALVES (NULL,19,'1','罗''''''] INSERT INTO USER(NAME,AGE,STATUS,GENDER) VALUES ('TOM3',19,'1','男'); INSERT INTO USER(NAME,AGE,STATUS,GENDER) VALUES ('TOM4',80,'1',' 男'); INSERT INTO USER(NAME,AGE,STATUS,GENDER) VALUES ('TOM5',-1,'1','男'); INSERT INTO USER(NAME,AGE,STATUS,GENDER) VALUES ('TOM5',121,'1','男'); INSERT INTO USER(NAME,AGE,GENDER) VALUES ('TOM5',120,'男'); -->
![](https://cdn.nlark.com/yuque/0/2025/png/52131016/1745631698560-cac9eb1d-c03e-4ddd-ae5d-c6d653582d55.png)







具有外键的是子表，与外键有所关联的表是父表







<!-- 这是一张图片，ocr 内容为：外键约束 语法 添加外键 CREATE TABLE表名( 字段名数据类型, [CONSTRAIN]][外键名称] FOREIGN KEY(外键字段名) REFERENCES 主表(主表(主表列名) ALTER TABLE 表名 ADD CONSTRAINT 外键名称 FOREIGN KEY(外键字段名) REFERENCES 主表列名) 删除外键 ALTERTABLE表名 DROP FOREIGN KEY 外键名称 -->
![](https://cdn.nlark.com/yuque/0/2025/png/52131016/1745631740317-47e20270-2f0f-463c-8ccd-64fed702dace.png)

<!-- 这是一张图片，ocr 内容为：外键约束 删除/更新行为 行为 说明 当在父表中删除(更新对应记录时,首先检查该记录是否有对应外键,如果有则不允许删除(更新.(与RESTRICT一致) NO ACTION RESTRICT 当在父表中刷除/更新对应记录时,首先检查该记录是否有对应外键,如果有则不允许删除(与NOACTION一致) 当在父表中删除/更新对应记录时,首先检查该记录是否有对应外键,如果有,则也删除/更新外键在子表中的记录. CASCADE 当在父表中国际对应记录时,首先检查该记录是否有对应外健,如果有则设置子表中该外键值为UL(这就要求该外键允许取NUL). SET  NULL 父表有变更时,子表将外键列设置成一个默认的值(INNODB不支持) SET  DEFAULT ALTERTABLE  ADOCONSTRONT 外黄名称 FOREGNKER (价领学院) 用FFERENGES 主表示度各(主要字度各(主要字度各) ONUPDATECADE ONDE OSCADE -->
![](https://cdn.nlark.com/yuque/0/2025/png/52131016/1745631766173-c0c2d553-8159-4614-9997-3755a7dc0fc4.png)

