---
title: 字符串常用操作
url: https://www.yuque.com/ehsuh/iwvo3r/us1vmgioauag7d5l
doc_id: 242040162
exported_at: 2026-09-12T10:33:49
---



<!-- 这是一张图片，ocr 内容为：操作 编号 说明 字符串[下标] 根据下标索引取出特定位置字符 字符串(INDEX(字符串) 查找给定字符的第一个匹配项的下标 2 将字符串内的全部字符串1,替换为字 字符串.REPLACE(字符串1,字符串2) 符串2 3 不会修改原字符串,而是得到一个新的 按照给定字符串,对字符串进行分隔 字符串.SPLIT(字符串) 不会修改原字符串,而是得到一个新的 列表 字符串.STRIP() 移除首尾的空格和换行符或指定字符串 5 字符串.STRIP(字符串) 字符串.COUNT(字符串) 统计字符串内某字符串的出现次数 9 LEN(字符串) 7 统计字符串的字符个数 -->
![](https://cdn.nlark.com/yuque/0/2025/png/52131016/1761452692330-faabe5c7-71f2-4d33-99b1-31718a72939d.png)



<!-- 这是一张图片，ocr 内容为：字符串的替换 语法:字符串.REPLACE(字符串1,字符串2) 功能:将字符串内的全部:字符串1 替换为字符串2 注意:不是修改字符串本身,而是得到了一个新字符串哦 -->
![](https://cdn.nlark.com/yuque/0/2025/png/52131016/1761452590284-a9b0acc2-d4b4-4b98-902d-2b6fb73ea5be.png)

<!-- 这是一张图片，ocr 内容为：字符串的规整操作(去前后空格) 语法:字符串.STRIP() ITHEIMA AND ITCAST MY-STR PRINT(MY-STR.STRIP() #结果:"ITHEIMA AND ITCAST" 字符串的规整操作(去前后指定字符串) 语法:字符串.STRIP(字符串) MY_STR "12ITHEIMA AND IT ITCAST21" #结果:"ITHEIMA AND ITCAST" PRINT(MY-STR.STRIP("12")) 其实就是:"1"和"2"都会移除,是按照单个字符. 注意,传入的是"12" -->
![](https://cdn.nlark.com/yuque/0/2025/png/52131016/1761452603090-fd25eee2-a448-43fa-af91-6ee4d4e0c869.png)



总结

<!-- 这是一张图片，ocr 内容为：1.字符串为什么被称之为数据容器呢? 字符串可以看做是字符的容器,支持下标索引等特性 2.字符串有哪些常用操作方法? 字符串[下标] 根据下标索引取出特定位置字符 字符串.INDEX(字符串) 查找给定字符的第一个匹配项的下标 将字符串内的全部字符串1,替换为字 字符串.REPLACE(字符串1,字符串2) 符串2 不会修改原字符串,而是得到一个新的 按照给定字符串,对字符串进行分隔 字符串.SPLIT(字符串) 不会修改原字符串,而是得到一个新的 列表 字符串.STRIP() 移除首尾的空格和换行符或指定字符串 字符串.STRIP(字符串) 统计字符串内某字符串的出现次数 字符串.COUNT(字符串) 统计字符串的字符个数 LEN(字符串) 3.字符串有哪些特点: 只可以存储字符串 长度任意(取决于内存大小) 支持下标索引 允许重复字符串存在 不可以修改(增加或删除元素等) 支持不标索引等特性 -->
![](https://cdn.nlark.com/yuque/0/2025/png/52131016/1761452727238-34841e23-d7a9-4ca1-a683-823da5630e9f.png)



