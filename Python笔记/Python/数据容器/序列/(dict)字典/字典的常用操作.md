---
title: 字典的常用操作
url: https://www.yuque.com/ehsuh/iwvo3r/vlvhcgcose2vz1nd
doc_id: 242083496
exported_at: 2026-09-12T10:33:39
---



<!-- 这是一张图片，ocr 内容为：编号 说明 操作 1275X 448 PX 获取指定KEY对应的VALUE值 字典[KEY] 字典[KEY]VALUE 2 添加或更新键值对 3 取出KEY对应的VALUE并在字典内删除此KEY的键值对 字典.POP(KEY) 清空字典 字典.CLEAR() 字典.KEYS() 获取字典的全部KEY,可用于FOR循环遍历字典 LEN(字典) 计算字典内的元素数量 -->
![](https://cdn.nlark.com/yuque/0/2025/png/52131016/1761483476444-587ca609-0722-4686-a3e9-e70d80fd05f0.png)



新增元素。

语法:字典恂墥key)=Value，结果:字典被修改，新增了元素

更新元素.

语法:字典[Key]=Value，结果:字典被修改，元素被更新

注意:字典Key不可以重复，所以对已存在的Key执行上述操作，就是更新Value值

删除元素

语法:字典.pop(Key)，结果:获得指定Key的Value，同时字典被修改，指定Key的数据被删除

清空字典

语法:字典.clear()，结果:字典被修改，元素被清空

获取全部的key

语法:字典.keys()，结果:得到字典中的全部Key



```java
for key in keys:
    print(f"字典的key是:{key}")
    print(f"字典的value是:{my_dict[key]}")
```



补充：

items方法

items方法就是能将键和值全部打印出来，比for循环方便

```plain
my_dict = {'a': 1, 'b': 2}
items_view = my_dict.items()  # 获取视图对象
print(items_view)  # 输出：dict_items([('a', 1), ('b', 2)])

# 修改原始字典
my_dict['c'] = 3

# 视图对象也随之更新
print(items_view)  # 输出：dict_items([('a', 1), ('b', 2), ('c', 3)])[5](@ref)

```

